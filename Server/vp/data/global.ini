# Globale Settings File

# Alle Dateipfade
[paths]
# wird dynamisch hinzugefügt
vp =
data = ${vp}/data
schedule = ${data}/schedule.json
parser = ${data}/parser.ini
hashes = ${data}/hashes.ini
tmp = ${data}/tmp.xml

[log]
file = ${paths:data}/server.log
mode = w
level = DEBUG
format = %(asctime)s [%(levelname)-8s] %(message)s
datefmt = %H:%M:%S
